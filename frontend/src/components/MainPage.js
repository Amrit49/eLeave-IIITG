import React from "react";
import NavBar from "./NavBar";

const MainPage = () => {
    return(
        <div className="main-page">
            <NavBar/>

        </div>
    );
};

export default MainPage;